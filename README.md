# QR-PROJECT
Converting the website URL to the QR Code 
YOGESH KUMAR DULOTIA
A simple Node.js project that generates a QR code image from a user-entered URL and saves the URL into a text file. This project uses Inquirer for user input, qr-image for QR code generation, and Node.js fs module for file handling.
